<div class="hero min-h-screen bg-gray-950">
  <div class="hero-content flex-col">
    <div class="card bg-gray-800 border border-gray-700 w-full max-w-md shadow-2xl">
      <div class="card-body">
        <h1 class="card-title text-2xl mb-4 text-gray-100">Update your password</h1>

        <% if alert = flash[:alert] %>
          <div class="alert alert-error mb-4" id="alert">
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span><%= alert %></span>
          </div>
        <% end %>

        <%= form_with url: password_path(params[:token]), method: :put, class: "space-y-4" do |form| %>
          <div class="form-control">
            <%= form.label :password, "New password", class: "label text-gray-300" %>
            <%= form.password_field :password, 
                required: true, 
                autocomplete: "new-password", 
                placeholder: "Enter new password", 
                maxlength: 72, 
                class: "input input-bordered w-full bg-gray-700 border-gray-600 text-gray-100 placeholder-gray-500" %>
          </div>

          <div class="form-control">
            <%= form.label :password_confirmation, "Confirm new password", class: "label text-gray-300" %>
            <%= form.password_field :password_confirmation, 
                required: true, 
                autocomplete: "new-password", 
                placeholder: "Repeat new password", 
                maxlength: 72, 
                class: "input input-bordered w-full bg-gray-700 border-gray-600 text-gray-100 placeholder-gray-500" %>
          </div>

          <div class="form-control mt-6">
            <%= form.submit "Save", class: "btn btn-primary w-full" %>
          </div>

          <div class="text-center mt-4">
            <%= link_to "Back to sign in", new_session_path, class: "link link-hover text-sm text-blue-400 hover:text-blue-300" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
