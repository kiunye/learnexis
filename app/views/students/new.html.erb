<% content_for :title, "New Student" %>

<div>
  <%# Page Header %>
  <div class="mb-6">
    <div class="breadcrumbs text-sm mb-2">
      <ul>
        <li><%= link_to "Students", students_path, class: "text-blue-600 hover:text-blue-700" %></li>
        <li>New Student</li>
      </ul>
    </div>
    <h1 class="text-3xl font-bold text-gray-900">New Student Admission</h1>
    <p class="text-gray-600 mt-1">Create a new student record and user account</p>
  </div>

  <%= render "form" %>
</div>
